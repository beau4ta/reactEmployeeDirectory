import React from 'react';
import { Children } from 'react';
import './wrapper.css';

const Wrapper = () => {
    return (
        <div className='wrapper'>
            { Children }
        </div>
    );
};

export default Wrapper;